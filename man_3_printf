.\" Man page for _printf
.TH _printf 3 "Nov 2023" "Holberton School"
.SH NAME
_printf \- Format and print data

.SH SYNOPSIS
.B #include "main.h"
.PP
.B int _printf(const char *format, ...);

.SH DESCRIPTION
The \fB_printf\fR function produces output according to a format. The format is a character string composed of zero or more directives. Supported conversion specifiers include %c, %s, %d, and %i. Flags, field width, precision, and length modifiers are not handled.

.SH RETURN VALUE
The \fB_printf\fR function returns the number of characters printed (excluding the null byte used to end output to strings).

.SH EXAMPLES
Print a string:
.B _printf("Hello, %s!", "world");

Print an integer:
.B _printf("The answer is %d", 42);

.SH SEE ALSO
.BR printf (3)

.SH AUTHOR
Written by OmarJaber.

.SH BUGS
Report bugs to <omarj819@gmail.com>.
